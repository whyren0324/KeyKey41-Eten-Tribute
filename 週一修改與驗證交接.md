# KeyKey 41：開機啟動與 Caps Lock 修正交接

日期：2026-08-08

## 本次處理範圍

1. Windows 登入後，TIP 已啟用但後端服務尚未建立 Named Pipe，造成短時間只能輸入英文。
2. 中文／繁體模式開啟 Caps Lock 後，`ToUnicode()` 產生大寫 ASCII，注音引擎無法辨識原本的實體字母鍵。

正確行為是 Caps Lock 只控制英文大小寫，不應將繁體中文輸入變成英文輸入。

## 已完成的原始碼修改

### TIP 啟用時補啟動後端服務

修改檔案：

- `src/Client/Globals.h`
- `src/Client/Globals.cpp`
- `src/Client/McBopomofoTIP.cpp`

新增 `EnsureServerStarted()`，並在 `McBopomofoTIP::ActivateEx()` 初期呼叫。函式先以零等待時間檢查 Named Pipe；若 pipe 已可使用或只是所有 instance 正忙，便不重複啟動。

服務未就緒時，函式從目前 TIP DLL 所在目錄依序尋找：

- x64 DLL：`McBopomofoServer_x64.exe`
- x86 DLL：`McBopomofoServer_x86.exe`
- 開發建置備援：`McBopomofoServer.exe`

找到後以 `CreateProcessW()` 啟動，working directory 設為 DLL 目錄。Server 本身既有的 single-instance mutex 負責處理多個程式同時啟用 TIP 的競速。

### 中文模式忽略 Caps Lock toggle 狀態

修改 `src/Client/McBopomofoTIP.cpp`：在中文模式的按鍵處理呼叫 `ToUnicode()` 前執行：

```cpp
keyboardState[VK_CAPITAL] &= 0x80;
```

這只清除 Caps Lock 的 toggle bit，保留 Shift 與 Caps Lock 實際按下狀態。英文模式在這段中文引擎處理之前便已返回，因此 Caps Lock 控制英文大小寫的行為不受影響。

## 建置與測試

建議使用新的 build 目錄，避免沿用其他電腦產生、含不同絕對路徑或 SDK 設定的 cache：

```powershell
cmake -S . -B .\build_company -A x64
cmake --build .\build_company --config Release --parallel 4
ctest --test-dir .\build_company -C Release --output-on-failure
```

正式安裝或製作 MSI：

```powershell
.\install.ps1
.\build_msi.ps1
```

執行安裝腳本前請儲存工作並關閉可能載入輸入法 DLL 的程式。

## 實機驗收清單

### Caps Lock

- [ ] 中文／繁體模式、Caps Lock OFF，可正常輸入中文。
- [ ] 中文／繁體模式、Caps Lock ON，以相同注音鍵序輸入相同中文字。
- [ ] Caps Lock ON 時候選、選字、空白及 Enter 正常。
- [ ] Caps Lock ON/OFF 均不影響左 Shift 中英切換。
- [ ] 英文模式的 Caps Lock 大小寫控制正常。
- [ ] Shift 加標點、Shift+Space、Ctrl+F3 等快捷鍵沒有退化。

### 開機啟動

- [ ] 安裝新版後登出再登入或重新開機。
- [ ] 桌面出現後立即在記事本切換 KeyKey 41，可直接輸入中文。
- [ ] 工作管理員只有一個對應架構的 `McBopomofoServer` 常駐實例。
- [ ] 原生 x64 與 x86/WOW64 程式皆可輸入中文。
- [ ] 手動結束 server 後，重新啟用 KeyKey 41 會再次啟動服務。

## 尚未進行：第二階段 Pipe 重試

目前只在 `ActivateEx()` 非同步補啟動服務。若實機仍偶爾漏掉桌面出現後的第一鍵，再考慮於第一次 Named Pipe 呼叫失敗時啟動服務、短暫等待 pipe ready，並只重試該請求一次。此方案仍在討論中，本次未實作；不得無限重試，以免卡住前景應用程式。
